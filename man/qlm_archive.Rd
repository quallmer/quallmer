% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qlm_trail.R
\name{qlm_archive}
\alias{qlm_archive}
\title{Archive quallmer workflow}
\usage{
qlm_archive(x, ..., path, report = TRUE)
}
\arguments{
\item{x}{Either a \code{qlm_trail} object from \code{\link[=qlm_trail]{qlm_trail()}}, or the first
quallmer object (\code{qlm_coded}, \code{qlm_comparison}, or \code{qlm_validation}).}

\item{...}{Additional quallmer objects to include in the trail (only used
when \code{x} is not a \code{qlm_trail} object).}

\item{path}{Base path for output files. Creates \verb{\{path\}.rds}, \verb{\{path\}.json},
and optionally \verb{\{path\}.qmd}.}

\item{report}{Logical. If \code{TRUE}, generates a Quarto report file.
Default is \code{TRUE}.}
}
\value{
Invisibly returns the \code{qlm_trail} object.
}
\description{
Convenience function that saves, exports, and optionally generates a report
for a quallmer workflow in one call. Accepts either coded objects directly
or a pre-built \code{qlm_trail} object.
}
\details{
This function creates a complete archive of your workflow:
\itemize{
\item \verb{\{path\}.rds}: Complete trail object for R (can be reloaded with \code{readRDS()})
\item \verb{\{path\}.json}: Portable metadata for archival or sharing
\item \verb{\{path\}.qmd}: Human-readable report (if \code{report = TRUE})
}

The function can be used in two ways:
\enumerate{
\item \strong{Standalone}: Pass coded objects directly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{qlm_archive(coded1, coded2, path = "workflow")
}\if{html}{\out{</div>}}
\item \strong{Piped}: Pass a pre-built trail:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{qlm_trail(coded1, coded2) |>
  qlm_archive(path = "workflow")
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Code movie reviews and create replication
coded1 <- qlm_code(data_corpus_LMRDsample, data_codebook_sentiment,
                   model = "openai/gpt-4o", name = "gpt4o_run")
coded2 <- qlm_replicate(coded1, model = "openai/gpt-4o-mini", name = "mini_run")

# Archive entire workflow in one call
qlm_archive(coded1, coded2, path = "workflow")

# Piped usage
qlm_trail(coded1, coded2) |>
  qlm_archive(path = "workflow")

# Without report
qlm_archive(coded1, coded2, path = "workflow", report = FALSE)
}

}
\seealso{
\code{\link[=qlm_trail]{qlm_trail()}}, \code{\link[=qlm_trail_save]{qlm_trail_save()}}, \code{\link[=qlm_trail_export]{qlm_trail_export()}}, \code{\link[=qlm_trail_report]{qlm_trail_report()}}
}
